# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# css
- Use CSS custom properties (--brand, --accent, --text, --muted, --bg, etc.) instead of hardcoded hex values for brand colors, backgrounds, and text colors. Confidence: 0.65
- Use `#0D609E` as the primary (deep blue) brand color and `#F08F1A` as the secondary (orange) brand color; reject colors from external reference designs that conflict. Confidence: 0.83
- Add comments in code instructing developers to always use brand colors when implementing reference designs, rather than following reference design colors directly. Confidence: 0.75

